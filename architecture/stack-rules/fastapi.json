{
  "stack": "fastapi",
  "description": "Stack-specific architecture overrides for FastAPI projects",

  "layer_mappings": {
    "app/models/": "types",
    "app/schemas/": "types",
    "app/crud/": "repo",
    "app/repositories/": "repo",
    "app/services/": "service",
    "app/api/": "runtime"
  },

  "overrides": {
    "allowed_cross_layer": []
  },

  "public_entry": "__init__.py",

  "notes": [
    "FastAPI models and schemas both map to the types layer",
    "CRUD modules and repositories map to the repo layer",
    "Business logic in app/services/ maps to the service layer",
    "API route handlers in app/api/ map to the runtime layer",
    "Every package must have __init__.py as the public entry point"
  ]
}
