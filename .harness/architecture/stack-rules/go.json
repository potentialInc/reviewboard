{
  "stack": "go",
  "description": "Stack-specific architecture overrides for Go projects",

  "layer_mappings": {
    "internal/types/": "types",
    "pkg/types/": "types",
    "internal/config/": "config",
    "internal/repo/": "repo",
    "internal/service/": "service",
    "cmd/": "runtime",
    "internal/handler/": "runtime"
  },

  "overrides": {
    "allowed_cross_layer": []
  },

  "public_entry": "main.go",

  "notes": [
    "Go types can live in internal/types/ or pkg/types/ (exported)",
    "Configuration loading lives in internal/config/",
    "Repository layer in internal/repo/ handles data access",
    "Business logic in internal/service/",
    "Entry points in cmd/ and HTTP handlers in internal/handler/ are the runtime layer",
    "Go uses package-level visibility; public entry is typically the package itself"
  ]
}
