# Development Dockerfile - runs next dev with hot reload
FROM node:20-alpine
WORKDIR /app

# Install dependencies
COPY package.json package-lock.json ./
RUN npm ci

# Copy source (volumes will overlay src/ for hot reload)
COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
